# Integrations moved

Customer setup for Artifactory, Nexus, and SonarQube lives in the sibling repo
**lightwell-integrations** (`../lightwell-integrations` when both trees are checked out
next to each other).

That repo is the shareable kit: proxy Lightwell, then (separately) grade the bump with
upgrade-delta. Do not copy those guides back into this tree — this repository is internal
and is not what you hand to a customer.
